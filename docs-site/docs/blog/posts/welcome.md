---
draft: true
date: 2026-08-27
authors:
  - isurki
---

# Placeholder post

This post only exists to verify the blog scaffolding renders correctly in local preview. Marked `draft: true`, so it's excluded automatically from any real build (`mkdocs build` / `mkdocs gh-deploy`) — visible only here, in `mkdocs serve`.
